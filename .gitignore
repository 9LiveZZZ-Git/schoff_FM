# Build outputs
build/
Builds/
*.obj
*.o
*.pdb
*.iobj
*.ipdb
*.ilk
*.res
*.log
*.tlog/
*.lib
*.exp

# Third party SDKs (download separately)
ThirdParty/

# IDE files
.vs/
*.user
*.suo
*.vcxproj.user

# WebView2 cache
*.WebView2/

# macOS
.DS_Store

# Gradle (Android)
.gradle/
local.properties

# Legacy/unused files (keep locally but don't track)
JuceLibraryCode/
Old HTMLs/
JUCE_FFT_Integration.h
JUCE_INTEGRATION_GUIDE.cpp
schoffhauzer_faceplate_final.html
setup webview2
setup_webview2.ps1
webview2 Juice
effect.h

# Claude settings
.claude/

# Allow release binaries for Modern version
!Schoff_FM Modern/*.exe
!Schoff_FM Modern/*.dll
!Schoff_FM Modern/*.vst3

# Old CSS version - include sources and binaries
!Schoff_FM (old CSS)/
Schoff_FM (old CSS)/schoff_stereo/Builds/
Schoff_FM (old CSS)/JuceLibraryCode_standalone/
!Schoff_FM (old CSS)/*.exe
!Schoff_FM (old CSS)/*.vst3
!Schoff_FM (old CSS)/schoff_stereo.vst3/
